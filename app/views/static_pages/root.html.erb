
<% if logged_in? %>
  <script type="text/javascript">
    window.currentUser = <%= render(
      "api/users/user.json.jbuilder",
      user: current_user
    ).html_safe %>;
    window.team = <%= render(
      "api/users/team.json.jbuilder",
      team: current_user.team
    ).html_safe %>;
  </script>
<% end %>

<div id="root">React is not working</div>